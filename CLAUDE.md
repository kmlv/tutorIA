# Claude Instructions

Follow `AGENTS.md`. For multi-agent coordination, also read
`coord/AGENTS_PROTOCOL.md`.

If `coord/AGENTS_PROTOCOL.md` exists in this repo, use the `coord` skill
before any nontrivial edits — call `coord_status` first, then post a `claim`
via `coord_post` before editing.

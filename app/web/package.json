{
  "name": "tutoria-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc src/player/sync.ts src/graph/state.ts src/graph/script.ts src/practice/loop.ts --outDir dist-test --rootDir src --target es2020 --module es2020 --moduleResolution node --skipLibCheck && node test/sync.invariant.mjs && node test/script.golden.mjs && node test/practice.contexto.mjs"
  },
  "devDependencies": {
    "katex": "^0.16.11",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}

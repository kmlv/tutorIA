{
  "name": "tutoria-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc src/player/sync.ts --outDir dist-test --target es2020 --module es2020 --moduleResolution node --skipLibCheck && node test/sync.invariant.mjs"
  },
  "devDependencies": {
    "katex": "^0.16.11",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
